{{ .Chart.Name }} deployed.

{{- if .Values.ingress.enabled }}
{{- $host := ternary (printf "%s.%s" .Values.ingress.subdomain .Values.ingress.domain) (printf "%s-%s.%s" .Values.ingress.subdomain .Values.ingress.environment .Values.ingress.domain) (eq .Values.ingress.environment "prod") }}

API base:   https://{{ $host }}/{{ .Values.ingress.pathPrefix }}
SignalR:    https://{{ $host }}/{{ .Values.ingress.pathPrefix }}/web-status
{{- end }}

Health:     kubectl exec deploy/{{ include "race-management-web-api.fullname" . }} -- wget -qO- localhost:{{ .Values.service.port }}/healthz/ready
