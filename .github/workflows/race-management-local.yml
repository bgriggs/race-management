name: race-management-local

on:
  push:
    branches: [main]
    paths:
      - 'ui/race-management-local/**'
      - '.github/workflows/race-management-local.yml'
  pull_request:
    paths:
      - 'ui/race-management-local/**'
      - '.github/workflows/race-management-local.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ui/race-management-local
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: ui/race-management-local/package-lock.json

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build

      - name: Test
        run: npm test -- --watch=false
